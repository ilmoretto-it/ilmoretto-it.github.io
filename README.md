# ILMORETTO-IT — Elegant Fashion Store V5 (Fixed)

Repaired GitHub Pages package for ILMORETTO-IT.

## Fixes in V5
- Added the ILMORETTO-IT logo to the header and footer with a reliable local fallback.
- Added a real favicon so the browser tab displays the brand mark instead of the generic globe icon.
- Added Apple touch icon and theme color.
- Kept all main images local to avoid broken image links.
- Added `404.html` for a friendlier GitHub Pages fallback.
- ZIP opens with `index.html` directly in the ZIP root (no extra wrapping folder).

## GitHub Pages upload
Upload **the files inside this ZIP directly to the repository root**. You should see `index.html`, `README.md`, `404.html`, `.nojekyll`, and `assets/` on the main repository page.

## Demo customer support
+1 (212) 555-0187

Replace demo contact details with your verified business information before a commercial launch.
